# SADeveloperReviewerAgent — SAM agent component config.

agent:
  name: SADeveloperReviewerAgent
  description: >
    Developer-perspective reviewer.

  agent_card:
    description: >
      Developer-perspective reviewer.
    defaultInputModes: ["text/plain", "application/json"]
    defaultOutputModes: ["text/plain", "application/json"]
    skills:
      - id: review_developer
        name: Developer Review
        description: Developer-perspective reviewer

  tools:
    - tool_type: python
      component_module: "solace_architect_core.tools.artifact_tools"
      function_name: "read_artifact"
    - tool_type: python
      component_module: "solace_architect_core.tools.artifact_tools"
      function_name: "list_artifacts"
    - tool_type: python
      component_module: "solace_architect_core.tools.decision_tools"
      function_name: "record_finding"
    - tool_type: python
      component_module: "solace_architect_core.tools.decision_tools"
      function_name: "read_decisions"
    - tool_type: python
      component_module: "solace_architect_core.tools.grounding_tools"
      function_name: "load_grounding"
    - tool_type: python
      component_module: "solace_architect_core.tools.grounding_tools"
      function_name: "fetch_canonical_source"

  system_prompt: |
    You are the **Developer-perspective reviewer**.

    ## Rubric
    1. Are topic names usable by developers (clear hierarchy, reasonable length, no ambiguity)?
    2. Are SDK / API choices appropriate for the team's language stack?
    3. Is schema governance defined (versioning, registry, evolution rules)?
    4. Are error handling paths defined (DLQ, retry, alerting)?
    5. Is the developer onboarding path clear (what to install, configure, test first)?

    ## Grounding
    Platform reference (Developer Tools, APIs, Schema Registry), canonical sources (API
    feature matrix, tutorials).

    ## Output
    reviews/developer-review.yaml
