# SADeveloperReviewerAgent — SAM agent component config (apps: form, converted 2026-05-19).
#
# Structural conversion from legacy `agent:` shape; system_prompt is ported
# verbatim from the legacy config. Per-agent prompt refactoring to v2's
# idioms (per-turn invariant, narration rules, ask_user_question + schema-
# echo, clickable-options-with-note) is a per-agent follow-up.

log:
  stdout_log_level: INFO
  log_file_level: DEBUG
  log_file: sa-reviewer-developer.log

shared_config:
  - broker_connection: &broker_connection
      dev_mode: ${SOLACE_DEV_MODE, false}
      broker_url: ${SOLACE_BROKER_URL, ws://localhost:8080}
      broker_username: ${SOLACE_BROKER_USERNAME, default}
      broker_password: ${SOLACE_BROKER_PASSWORD, default}
      broker_vpn: ${SOLACE_BROKER_VPN, default}
      temporary_queue: ${USE_TEMPORARY_QUEUES, true}

  - models:
      general: &general_model
        model: ${LLM_SERVICE_GENERAL_MODEL_NAME}
        api_base: ${LLM_SERVICE_ENDPOINT}
        api_key: ${LLM_SERVICE_API_KEY}

  - services:
      session_service: &default_session_service
        type: "memory"
        default_behavior: "PERSISTENT"

      artifact_service: &default_artifact_service
        type: "filesystem"
        base_path: ${SA_STORAGE_ROOT, /tmp/sa-artifacts}
        artifact_scope: namespace

apps:
  - name: sa-reviewer-developer-app
    app_base_path: .
    app_module: solace_agent_mesh.agent.sac.app
    broker:
      <<: *broker_connection

    app_config:
      namespace: ${NAMESPACE}
      supports_streaming: true
      agent_name: "SADeveloperReviewerAgent"
      display_name: "SADeveloperReviewerAgent"
      # Opt out of the framework's artifact_management builtin group so the
      # core's engagement-scoped list_artifacts wins under the same name.
      auto_inject_artifact_tools: false
      model_provider:
        - "planning"

      agent_card_publishing:
        interval_seconds: 10
      agent_discovery:
        enabled: true
      inter_agent_communication:
        allow_list: []   # reviewers are non-interactive; no peer dispatch
        deny_list: []
        request_timeout_seconds: 60

      # Forcing the plugin package to import on agent boot — activates the
      # per-plugin log handler AND installs the after_model_callback
      # telemetry patch (idempotent across agents in the same process).
      agent_init_function:
        module: solace_architect_reviewer_developer.lifecycle
        name: init
      agent_cleanup_function:
        module: solace_architect_reviewer_developer.lifecycle
        name: cleanup

      instruction: |
        # Role
        SADeveloperReviewerAgent — developer-usability reviewer. Read SADomainAgent's
        design artifacts, audit, record findings, write narrative, end the turn.
        NON-INTERACTIVE: never ask the user.

        # Discipline
        Assert only what Solace docs support. Prefer `load_grounding(topic)` over recall;
        on gap call `record_grounding_gap`. Voice: senior developer peer review, direct.
        Use Solace terms (event broker, Direct/Guaranteed, DMR, Micro-Integration, Event
        Portal, SAM, entrypoint). <!-- TERMINOLOGY-RULE-START -->Forbidden: "connector", "QoS", "gateway",
        "orchestrator agent" (two words).<!-- TERMINOLOGY-RULE-END -->

        Load during audit: `load_grounding("platform-reference")` (Developer Tools/APIs/
        Schema Registry/SDKs); `fetch_canonical_source(<url>)` for API matrix/tutorials
        when grounding gaps exist.

        # Rubric (5 lenses, per artifact)
        1. Topic usability — clear hierarchy, reasonable length, no UUID-like or 8+ level
           topics.
        2. SDK/API choice — appropriate for language stack and integration pattern.
        3. Schema governance (5 sub-checks against Schema Registry section):
           a. Concrete schemas (Avro/JSON Schema/Protobuf) attached to each event →
              missing = critical.
           b. Registry-managed (Solace Schema Registry or equivalent) vs in-code →
              critical when design has >1 producer/consumer.
           c. Compatibility policy named (BACKWARD/FORWARD/FULL/NONE), per-event where
              needed → important if missing.
           d. SERDES integrated with registry (schema-id in message) vs hand-marshal →
              important with registry, critical without.
           e. Self-hosted deployment shape + auth (Basic/OIDC) called out → advisory if
              missing.
        4. Error-handling paths — DLQ, retry, poison-pill, consumer-lag alerting.
        5. Onboarding path — explicit "install/configure/test first" steps; "read the
           reference" is not an onboarding path.

        # Workflow (one task = one audit)
        1. `list_artifacts(engagement_id)` (status: "Reading design artifacts…").
        2. `read_decisions(engagement_id)` (status: "Reviewing decisions…").
        3. `read_artifact(...)` for each design md/yaml present under topic-design/,
           broker-select/, protocol-select/, integration/, mesh-design/, ha-dr/, sam-design/,
           event-portal/, migration/ (status: "Auditing <scope>…").
        4. Load grounding topics above (status: "Loading <topic> grounding…").
        5. For each issue: `record_finding(severity=<critical|important|advisory>,
           description=..., affected_artifact=..., recommendation=...,
           source_agent="SADeveloperReviewerAgent")`. Status: "Recording finding <N>…".
        6. `write_artifact(engagement_id, "reviews/developer-review.md", <content>)`.
           Status: "Writing developer-review.md…".
        7. End turn with summary + Completion Status block.

        # Narrative format — reviews/developer-review.md
        Sections: Verdict · Findings summary (counts) · Strengths (1-3 bullets) ·
        Concerns (### F<id> — <sev>: <summary>, **Affected:**, **Issue:**,
        **Recommendation:**) · Out of scope (architect → SAArchitectReviewerAgent, ops →
        SAOpsReviewerAgent, security → SASecurityReviewerAgent).

        # Narration
        Emit `agent_status_message` embeds before EVERY tool call, ≤80 chars, present-tense,
        ending with "…". First-turn FIRST emission must be a status before any tool call.
        Never chain >3 tool calls without an intervening status.

        # Per-turn invariant
        Every turn ends with one of: `record_finding(...)` + status, `write_artifact(...)`,
        or final "review complete" text + Completion Status block.

        # Completion Status (final turn)
        ```
        STATUS: <DONE | DONE_WITH_CONCERNS | BLOCKED>
        REASON: <≤120 chars>
        ATTEMPTED: <e.g. "audited 6 design artifacts against 5-criterion rubric">
        RECOMMENDATION: <next step for orchestrator>
        ```
        Never NEEDS_CONTEXT. Missing design artifacts → record one critical finding, write
        minimal review noting "design phase has not run", return BLOCKED.

      tools:
        - tool_type: python
          component_module: "solace_architect_core.tools.artifact_tools"
          function_name: "read_artifact"
        - tool_type: python
          component_module: "solace_architect_core.tools.artifact_tools"
          function_name: "list_artifacts"
        - tool_type: python
          component_module: "solace_architect_core.tools.artifact_tools"
          function_name: "write_artifact"
        - tool_type: python
          component_module: "solace_architect_core.tools.decision_tools"
          function_name: "record_finding"
        - tool_type: python
          component_module: "solace_architect_core.tools.decision_tools"
          function_name: "read_decisions"
        - tool_type: python
          component_module: "solace_architect_core.tools.decision_tools"
          function_name: "record_open_item"
        - tool_type: python
          component_module: "solace_architect_core.tools.decision_tools"
          function_name: "record_feedback"
        - tool_type: python
          component_module: "solace_architect_core.tools.grounding_tools"
          function_name: "load_grounding"
        - tool_type: python
          component_module: "solace_architect_core.tools.grounding_tools"
          function_name: "fetch_canonical_source"
        - tool_type: python
          component_module: "solace_architect_core.tools.grounding_tools"
          function_name: "load_preamble"
        - tool_type: python
          component_module: "solace_architect_core.tools.grounding_tools"
          function_name: "load_jargon_list"
        - tool_type: python
          component_module: "solace_architect_core.tools.grounding_tools"
          function_name: "record_grounding_gap"

      session_service: *default_session_service
      artifact_service: *default_artifact_service
      artifact_handling_mode: "embed"
      enable_embed_resolution: true
      enable_artifact_content_instruction: true

      agent_card:
        description: 'Developer-perspective reviewer.

          '
        defaultInputModes:
        - text/plain
        - application/json
        defaultOutputModes:
        - text/plain
        - application/json
        skills:
        - id: review_developer
          name: Developer Review
          description: Developer-perspective reviewer
