# Node toolchain
node_modules/
*.tsbuildinfo
.vite/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Stray vite builds — the canonical build goes to
# ../src/solace_architect_webui_entrypoint/webui/visualizer/ (see vite.config.ts).
# This catches accidental local `vite build` runs without the configured outDir.
dist/

# OS noise
.DS_Store

# package-lock.json IS committed — reproducible installs.
